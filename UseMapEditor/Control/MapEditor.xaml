<UserControl x:Class="UseMapEditor.Control.MapEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UseMapEditor.Control" xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             GotFocus="UserControl_GotFocus"
             SizeChanged="UserControl_SizeChanged"
             MouseEnter="UserControl_MouseEnter"
             d:DesignHeight="450" d:DesignWidth="800">

    <DockPanel>
        <ToolBarTray DockPanel.Dock="Top" IsLocked="True">
            <ToolBar 
    Style="{DynamicResource MaterialDesignToolBar}"
    ClipToBounds="False">
                <Button
      ToolTip="Save">
                    <materialDesign:PackIcon
        Kind="ContentSave" />
                </Button>
                <Separator />
                <Button
      Command="Cut"
      ToolTip="Cut"
      ToolBar.OverflowMode="AsNeeded">
                    <materialDesign:PackIcon
        Kind="ContentCut" />
                </Button>
                <Button
      Command="Copy"
      ToolTip="Copy that stuff"
      ToolBar.OverflowMode="AsNeeded">
                    <materialDesign:PackIcon
        Kind="ContentCopy" />
                </Button>
                <Separator />
                <Button
      Command="Paste"
      ToolTip="Paste some stuff"
      ToolBar.OverflowMode="AsNeeded">
                    <materialDesign:PackIcon
        Kind="ContentPaste" />
                </Button>
                <!-- when badging in a toolbar, make sure the parent ToolBar.ClipToBounds="False", and
                   manually apply the button style -->
                <materialDesign:Badged
      ToolBar.OverflowMode="AsNeeded"
      Badge="{materialDesign:PackIcon Alert}"
      CornerRadius="5">
                    <Button
        ToolTip="Badge it up!"
        Style="{StaticResource {x:Static ToolBar.ButtonStyleKey}}">
                        <materialDesign:PackIcon
          Kind="AirplaneTakeoff" />
                    </Button>
                </materialDesign:Badged>
                <Separator />
                <ListBox>
                    <ListBoxItem
        ToolTip="This is a lonley toggle with TextBlock instead of icon">
                        <TextBlock>
        W
                        </TextBlock>
                    </ListBoxItem>
                </ListBox>
                <Separator />
                <ListBox
      SelectedIndex="0">
                    <ListBox.ToolTip>
                        <StackPanel>
                            <TextBlock
            Text="MaterialDesignToolToggleFlatListBox" />
                            <TextBlock
            Text="Exclusive selection" />
                            <TextBlock
            Text="ListBoxAssist.IsToggle allows more natural toggle behaviour" />
                        </StackPanel>
                    </ListBox.ToolTip>
                    <ListBoxItem>
                        <materialDesign:PackIcon
          Kind="FormatAlignLeft" />
                    </ListBoxItem>
                    <ListBoxItem>
                        <materialDesign:PackIcon
          Kind="FormatAlignCenter" />
                    </ListBoxItem>
                    <ListBoxItem>
                        <materialDesign:PackIcon
          Kind="FormatAlignRight" />
                    </ListBoxItem>
                    <ListBoxItem>
                        <materialDesign:PackIcon
          Kind="FormatAlignJustify" />
                    </ListBoxItem>
                </ListBox>
                <Separator />
                <ListBox
      SelectionMode="Extended">
                    <ListBox.ToolTip>
                        <StackPanel>
                            <TextBlock
            Text="MaterialDesignToolToggleListBox" />
                            <TextBlock
            Text="Multiple selection" />
                            <TextBlock
            Text="ListBoxAssist.IsToggle allows more natural toggle behaviour" />
                        </StackPanel>
                    </ListBox.ToolTip>
                    <ListBoxItem>
                        <materialDesign:PackIcon
          Kind="FormatBold" />
                    </ListBoxItem>
                    <ListBoxItem>
                        <materialDesign:PackIcon
          Kind="FormatItalic" />
                    </ListBoxItem>
                    <ListBoxItem
        x:Name="UnderlineCheckbox">
                        <materialDesign:PackIcon
          Kind="FormatUnderline" />
                    </ListBoxItem>
                </ListBox>
                <Separator />
                <Label
      Content="Font size:"
      VerticalAlignment="Center" />
                <ComboBox>
                    <ComboBoxItem
        Content="10" />
                    <ComboBoxItem
        IsSelected="True"
        Content="12" />
                    <ComboBoxItem
        Content="14" />
                    <ComboBoxItem
        Content="16" />
                </ComboBox>
                <CheckBox>
                    Check
                </CheckBox>
                <Button
      ToolTip="Take a nap"
      ToolBar.OverflowMode="Always">
                    <materialDesign:PackIcon
        Kind="Hotel" />
                </Button>
                <RadioButton
      GroupName="XXX"
      Content="Radio" />
                <RadioButton
      GroupName="XXX"
      Content="Ga Ga" />
                <Separator />
                <RadioButton
      GroupName="YYY"
      Style="{StaticResource MaterialDesignToolRadioButton}">
                    <materialDesign:PackIcon
        Kind="Radio" />
                </RadioButton>
                <RadioButton
      GroupName="YYY"
      Style="{StaticResource MaterialDesignToolRadioButton}">
                    <materialDesign:PackIcon
        Kind="EmoticonPoop" />
                </RadioButton>
                <Separator />
                <ToggleButton />
            </ToolBar>
        </ToolBarTray>


        <Expander DockPanel.Dock="Bottom" ExpandDirection="Up" Header="콘솔">
            <DockPanel>
                <TextBox MinHeight="100" AcceptsReturn="True"/>
            </DockPanel>
        </Expander>
        <Expander DockPanel.Dock="Left" ExpandDirection="Right">
            <Expander.Header>
                <TextBlock Text="시나리오설정" RenderTransformOrigin=".5,.5" >
                    <TextBlock.LayoutTransform>
                        <RotateTransform Angle="90" />
                    </TextBlock.LayoutTransform>
                </TextBlock>
            </Expander.Header>
            <DockPanel>
                <ListBox>
                    <ListBoxItem Content="맵"/>
                    <ListBoxItem Content="세력"/>
                    <ListBoxItem Content="유닛"/>
                    <ListBoxItem Content="업그레이드"/>
                    <ListBoxItem Content="테크"/>
                    <ListBoxItem Content="사운드"/>
                    <ListBoxItem Content="스트링"/>
                </ListBox>
            </DockPanel>
        </Expander>
        <Expander DockPanel.Dock="Right" ExpandDirection="Left">
            <Expander.Header>
                <TextBlock Text="팔레트" RenderTransformOrigin=".5,.5">
                    <TextBlock.LayoutTransform>
                        <RotateTransform Angle="-90" />
                    </TextBlock.LayoutTransform>
                </TextBlock>
            </Expander.Header>
            <DockPanel>
                <Grid Width="256" Height="256" DockPanel.Dock="Top" Background="Black">
                    
                </Grid>
                <ListBox>

                </ListBox>
            </DockPanel>
        </Expander>

        
        
        <Grid>
            <Image x:Name="MapTrace"/>
            <Grid x:Name="MapViewer" Background="#33000000" >

            </Grid>
        </Grid>
    </DockPanel>
</UserControl>
